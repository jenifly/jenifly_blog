
const state = {
  colors: {},
}

const mutations = {
  SET_COLORS: (state, colors) => {
    state.colors = colors
  }
}

const actions = {
 
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
